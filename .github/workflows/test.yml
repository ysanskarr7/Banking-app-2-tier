name : Test Workflow

on:
  push:
    branches:
      - main

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name : Echo MEssage
        run : echo "Github Action is working successfully ✅"