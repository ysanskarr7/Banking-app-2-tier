version: "3.9"

services:
  simplebank:
    image: 972293797085.dkr.ecr.us-west-1.amazonaws.com/flaskapp-simple-bank:${IMAGE_TAG:-latest}
    container_name: simplebank
    ports:
      - "5000:5000"
    environment:
      - "FLASK_ENV=production"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s